# Frontend (exposed to the browser)
VITE_APP_NAME=Builder Copilot

# Server-side only — never exposed to the frontend bundle.
# Read by api/analyze.ts at request time.
AI_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Reserved for the next provider (not yet wired):
# ANTHROPIC_API_KEY=
